procedure provaid is
	
	type rang is new integer range 1..10;
	type rec is array(rang) of integer;

	coleccio : rec;
	procedure mnm(a: out integer) is
		b : integer;
	begin
		--puti(a);
		
		puti(a);
		a := a+3;
	end mnm;

	
begin
    coleccio(5) := 3;
	mnm(coleccio(1));
	new_line;
	puti(coleccio(1));
	
end provaid;
